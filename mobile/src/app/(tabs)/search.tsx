import { colors, radii, spacing } from "@smaj/design-tokens";
import { router } from "expo-router";
import { useMemo, useState } from "react";
import { Pressable, StyleSheet, Text, TextInput, View } from "react-native";
import { Screen } from "@/components/screen";
import { services } from "@/lib/services";
export default function SearchScreen(){const [query,setQuery]=useState("");const results=useMemo(()=>services.filter(item=>`${item.name} ${item.description}`.toLowerCase().includes(query.trim().toLowerCase())),[query]);return <Screen eyebrow="DISCOVER SMAJ" title="Search"><TextInput autoCapitalize="none" autoCorrect={false} value={query} onChangeText={setQuery} placeholder="Search services, products, jobs..." placeholderTextColor={colors.textMuted} style={styles.input}/><View style={styles.results}>{results.map(item=><Pressable style={styles.row} key={item.slug} onPress={()=>router.push({pathname:"/service/[slug]",params:{slug:item.slug}})}><Text style={styles.icon}>{item.icon}</Text><View style={styles.copy}><Text style={styles.name}>{item.name}</Text><Text numberOfLines={1} style={styles.description}>{item.description}</Text></View><Text style={styles.arrow}>&gt;</Text></Pressable>)}{!results.length?<Text style={styles.empty}>No service matches your search.</Text>:null}</View></Screen>}
const styles=StyleSheet.create({input:{height:52,paddingHorizontal:spacing.lg,borderRadius:radii.md,backgroundColor:colors.surface,borderWidth:1,borderColor:colors.border,color:colors.text,fontSize:15},results:{gap:spacing.sm},row:{minHeight:70,padding:spacing.md,flexDirection:"row",alignItems:"center",gap:spacing.md,borderRadius:radii.md,backgroundColor:colors.surface,borderWidth:1,borderColor:colors.border},icon:{fontSize:30},copy:{flex:1},name:{color:colors.text,fontWeight:"800",fontSize:15},description:{color:colors.textMuted,fontSize:12,marginTop:3},arrow:{color:colors.primary,fontSize:28},empty:{padding:spacing.xl,textAlign:"center",color:colors.textMuted}});