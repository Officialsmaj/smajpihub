import { useState } from "react";
import VisibilityOutlinedIcon from "@mui/icons-material/VisibilityOutlined";
import VisibilityOffOutlinedIcon from "@mui/icons-material/VisibilityOffOutlined";
import RefreshOutlinedIcon from "@mui/icons-material/RefreshOutlined";
import { useAuthContext } from "../../contexts/AuthContext";
const WalletPage = () => { const { user } = useAuthContext(); const [connected, setConnected] = useState(true); const [hidden, setHidden] = useState(true); const [refreshing, setRefreshing] = useState(false); const refresh = () => { setRefreshing(true); window.setTimeout(() => setRefreshing(false), 700); }; return <main className="private-page"><section className="private-page-head"><div><p className="private-kicker">PI WALLET</p><h1>Wallet</h1><p>Manage the wallet connection used by your SMAJ account.</p></div></section><section className="wallet-panel"><div><span>Connection status</span><strong className={connected ? "wallet-connected" : "wallet-disconnected"}>{connected ? "Connected" : "Disconnected"}</strong><p>{connected ? `Linked to @${user?.piUsername || user?.username}` : "No wallet connected on this device."}</p></div><div className="wallet-balance"><span>Balance</span><strong>{hidden ? "•••••• Pi" : "Available in Pi Wallet"}</strong><button onClick={() => setHidden((value) => !value)}>{hidden ? <VisibilityOutlinedIcon /> : <VisibilityOffOutlinedIcon />}{hidden ? "Show balance" : "Hide balance"}</button></div><div className="form-actions"><button className="private-primary-button" onClick={() => setConnected(true)} disabled={connected}>Connect Wallet</button><button className="private-secondary-button" onClick={refresh} disabled={!connected || refreshing}><RefreshOutlinedIcon />{refreshing ? "Refreshing..." : "Refresh"}</button><button className="private-secondary-button danger" onClick={() => setConnected(false)} disabled={!connected}>Disconnect</button></div></section></main>; };
export default WalletPage;
