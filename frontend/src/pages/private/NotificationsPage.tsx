import { useEffect, useState } from "react";
import { axiosClient } from "../../lib/axiosClient";
import type { AppNotification } from "../../types/marketplace";
const NotificationsPage = () => { const [items, setItems] = useState<AppNotification[]>([]); useEffect(() => { axiosClient.get("/notifications").then(({ data }) => setItems(data.notifications)); }, []); const open = async (item: AppNotification) => { if (!item.read) { await axiosClient.patch(`/notifications/${item._id}/read`); setItems((all) => all.map((entry) => entry._id === item._id ? { ...entry, read: true } : entry)); } }; const all = async () => { await axiosClient.patch("/notifications/read-all"); setItems((current) => current.map((item) => ({ ...item, read: true }))); }; return <main className="private-page"><section className="private-page-head"><div><p className="private-kicker">ACTIVITY</p><h1>Notifications</h1><p>Orders, payments, messages, moderation, and report updates.</p></div><button className="private-secondary-button" onClick={() => void all()}>Mark all as read</button></section><section className="notification-list">{items.length ? items.map((item) => <button key={item._id} className={item.read ? "" : "unread"} onClick={() => void open(item)}><span className="notification-dot" /><div><strong>{item.title}</strong><p>{item.message}</p><small>{new Date(item.createdAt).toLocaleString()}</small></div></button>) : <div className="private-state"><h2>You are all caught up</h2><p>Marketplace updates will appear here.</p></div>}</section></main>; };
export default NotificationsPage;
