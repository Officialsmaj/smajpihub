import { useEffect, useState } from "react";
import { Link, useNavigate, useParams } from "react-router-dom";
import ArrowBackIcon from "@mui/icons-material/ArrowBack"; import FlagOutlinedIcon from "@mui/icons-material/FlagOutlined"; import ChatOutlinedIcon from "@mui/icons-material/ChatOutlined"; import FavoriteBorderIcon from "@mui/icons-material/FavoriteBorder"; import FavoriteIcon from "@mui/icons-material/Favorite";
import { isAxiosError } from "axios";
import { axiosClient } from "../../lib/axiosClient"; import type { Product, SellerSummary } from "../../types/marketplace"; import { useAuthContext } from "../../contexts/AuthContext"; import TrustBadge from "../../components/TrustBadge"; import MarketplaceProductCard from "../../components/MarketplaceProductCard";
const ProductDetailPage = () => { const { id } = useParams(); const { user } = useAuthContext(); const navigate = useNavigate(); const [product, setProduct] = useState<Product | null>(null); const [seller, setSeller] = useState<SellerSummary | null>(null); const [related, setRelated] = useState<Product[]>([]); const [saved, setSaved] = useState(false); const [selectedImage, setSelectedImage] = useState(""); const [error, setError] = useState(""); const [message, setMessage] = useState(""); const [submitting, setSubmitting] = useState(false);
  useEffect(() => { axiosClient.get(`/marketplace/products/${id}`).then(({ data }) => { setProduct(data.product); setSeller(data.seller); setRelated(data.related || []); setSaved(Boolean(data.saved)); setSelectedImage(data.product.images?.[0] || data.product.image); }).catch(() => setError("Product not found.")); }, [id]);
  const action = async (kind: "order" | "report" | "message" | "favorite") => { if (!product) return; setSubmitting(true); setError(""); try { if (kind === "order") { await axiosClient.post("/marketplace/orders", { productId: product._id }); navigate("/orders", { state: { message: "Order created successfully." } }); } else if (kind === "favorite") { const { data } = await axiosClient.post(`/marketplace/products/${product._id}/favorite`); setSaved(data.saved); } else if (kind === "message") { const { data } = await axiosClient.post("/messages/start", { productId: product._id }); navigate(`/messages?conversation=${data.conversation._id}`); } else { const reason = window.prompt("Why are you reporting this product?", "Misleading or inappropriate listing"); if (reason?.trim()) { await axiosClient.post(`/marketplace/products/${product._id}/report`, { reason: reason.trim() }); setMessage("Product report submitted for admin review."); } } } catch (err: unknown) { setError(isAxiosError<{ message?: string }>(err) ? err.response?.data?.message || "Action could not be completed." : "Action could not be completed."); } finally { setSubmitting(false); } };
  if (!product) return <main className="private-page"><div className="private-state">{error || "Loading product..."}</div></main>; const images = product.images?.length ? product.images : [product.image].filter(Boolean);
  return <main className="private-page"><Link to="/store" className="private-back-link"><ArrowBackIcon /> Back to Store</Link><section className="product-detail"><div className="product-gallery"><div className="product-detail-image">{selectedImage ? <img src={selectedImage} alt={product.title} /> : <span>No image supplied</span>}</div>{images.length > 1 ? <div className="gallery-thumbnails">{images.map((image) => <button className={selectedImage === image ? "active" : ""} key={image.slice(-30)} onClick={() => setSelectedImage(image)}><img src={image} alt="" /></button>)}</div> : null}</div><div className="product-detail-content"><span className="product-category inline">{product.category}</span><h1>{product.title}</h1><p className="product-detail-price">{product.pricePi} Pi</p><p>{product.description}</p><Link className="seller-info-card" to={`/seller/${product.sellerId}`}><div className="profile-avatar small">{product.sellerName.slice(0, 1)}</div><div><span>Seller</span><strong>{seller?.displayName || product.sellerName}</strong><p>@{seller?.piUsername || product.piUsername} · {product.location}</p><TrustBadge level={seller?.verificationLevel} /></div></Link>{message ? <div className="private-alert success">{message}</div> : null}{error ? <div className="private-alert error">{error}</div> : null}{product.sellerId === user?.uid ? <p className="private-alert">This is your listing.</p> : <div className="product-detail-actions"><button className="private-primary-button" onClick={() => void action("order")} disabled={submitting}>Create Order</button><button className="private-secondary-button" onClick={() => void action("message")}><ChatOutlinedIcon /> Message Seller</button><button className="private-secondary-button" onClick={() => void action("favorite")}>{saved ? <FavoriteIcon /> : <FavoriteBorderIcon />}{saved ? "Saved" : "Save"}</button><button className="private-secondary-button" onClick={() => void action("report")}><FlagOutlinedIcon /> Report</button></div>}</div></section>{related.length ? <><section className="section-title"><div><h2>Related products</h2><p>More listings in {product.category}</p></div></section><section className="product-grid">{related.map((item) => <MarketplaceProductCard product={item} key={item._id} />)}</section></> : null}</main>;
}; export default ProductDetailPage;
